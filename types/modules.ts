export enum AdminModules {
  home = "/",
  users = "/users",
  analytics = "/analytics",
  config = "/config",
}

export enum UserModules {
  home = "/",
  profile = "/profile",
  config = "/config",
}

export enum BusinessModules {
  account = "/account",
  config = "/config",
  employees = "/employees",
  home = "/",
  metrics = "/metrics",
  myEcard = "/myEcard",
}
