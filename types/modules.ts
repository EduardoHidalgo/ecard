export enum AdminModules {
  home = "/",
  users = "/users",
  analytics = "/analytics",
  config = "/config",
}

export enum UserModules {
  home = "/",
  profile = "/profile",
  config = "/config",
}

export enum BusinessModules {
  home = "/",
  account = "/account",
  employees = "/employees",
  metrics = "/metrics",
  config = "/config",
}
