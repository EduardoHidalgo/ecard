export enum AdminModules {
  home = "/",
  users = "/users",
  analytics = "/analytics",
  config = "/config",
}

export enum UserModules {
  home = "/",
  profile = "/profile",
  metrics = "/metrics",
  config = "/config",
}
